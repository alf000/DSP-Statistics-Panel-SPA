#!usr/bin/expect -f"
spawn heroku login -i
expect -exact "heroku: Enter your login credentials"
expect -exact "Email:"
send -- "${{ secrets.HEROKU_EMAIL }}"
expect -exact "Password:"
send -- "${{ secrets.HEROKU_PASSWD }}"
expect eof
