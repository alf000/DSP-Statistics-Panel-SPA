#!usr/bin/expect -f"
spawn heroku login -i
expect -exact "heroku: Enter your login credentials"
expect -exact "Email:"
send -- "alf\@herland.as"
expect -exact "Password:"
send -- "\$\{\{ secrets.HEROKU_PASSWD \}\}\r"
expect eof
