#!usr/bin/expect -f"
spawn heroku login -i
expect -exact "heroku: Enter your login credentials"
expect -exact "Email:"
send -- "$HEROKU_EMAIL\r"
expect -exact "Password:"
send -- "$HEROKU_PASSWD\r"
expect eof
